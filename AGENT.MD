# AGENT.MD

本文件是给后续 IDE Agent / 编码 Agent / 自动修复 Agent 的长期协作规则。

它不是项目当前状态说明书，也不是阶段报告。
项目当前状态、阶段目标、测试基线、mock/真实能力边界，应以 `docs/PROJECT_STATUS.md` 和 `docs/PHASE_CURRENT.md` 为准。

Agent 修改工程前必须先阅读本文件，并按本文件的流程查阅当前阶段文档。

---

## 1. 文件职责

### 1.1 本文件负责什么

`AGENT.MD` 负责定义长期稳定的 Agent 行为规则，包括：

* 如何理解工程边界
* 如何判断任务类型
* 如何控制修改范围
* 如何处理 mock 与真实能力
* 如何运行测试
* 如何更新文档
* 如何输出复盘
* 遇到不确定情况时如何处理

### 1.2 本文件不负责什么

`AGENT.MD` 不应长期硬编码以下内容：

* 当前阶段编号
* 当前测试数量
* 当前具体 bug
* 当前页面重构范围
* 当前临时风险清单
* 当前 release candidate 路径
* 当前某个功能是否刚修完

这些信息应写入：

* `docs/PROJECT_STATUS.md`
* `docs/PHASE_CURRENT.md`
* `docs/dev-records/*`
* `docs/DECISIONS/*`（必要时新建）

---

## 2. Agent 启动流程

任何 Agent 接手本工程时，必须按以下顺序阅读：

1. `AGENT.MD`
2. `README.md`
3. `工程维护方案.md`
4. `docs/PROJECT_STATUS.md`
5. `docs/PHASE_CURRENT.md`
6. 与当前任务相关的页面、云函数、测试和文档

如果以上文件之间存在冲突，先判断冲突类型。

长期行为规则、协作流程、修改边界的优先级如下：

```text
用户当前明确指令
> AGENT.MD
> 工程维护方案.md
> docs/ui-refactor-guardrails.md
> README.md
> 历史阶段记录
```

当前工程事实、阶段目标、测试基线、mock/真实能力状态的优先级如下：

```text
用户当前明确指令
> docs/PHASE_CURRENT.md
> docs/PROJECT_STATUS.md
> README.md
> 历史阶段记录
```

如果冲突涉及支付、退款、提现、分账、认证、风控、权限、发布安全，Agent 必须暂停并说明冲突，不得擅自选择高风险路径。若当前事实文档疑似过期，应以代码、测试和最近阶段记录核实后再更新文档。

---

## 3. 工程定位

本工程是微信原生小程序 + 微信云开发项目。

常规结构：

```text
miniprogram/              小程序前端
cloudfunctions/           微信云函数
cloudfunctions/_shared/   云函数共享工具
schema/                   数据集合结构
docs/                     工程文档
tests/                    自动化测试
scripts/                  工程检查脚本
```

Agent 不应假设当前业务状态。
当前业务阶段、已完成功能、mock 能力、真实能力、测试基线，必须从 `docs/PROJECT_STATUS.md` 和 `docs/PHASE_CURRENT.md` 获取。

---

## 4. 任务分类

修改前先判断任务属于哪一类：

* 工程维护
* bug 修复
* UI 重构
* 文档更新
* 测试补充
* 业务功能开发
* 支付/退款/财务相关改动
* 认证/资质/风控相关改动
* 权限/角色相关改动
* 发布/交付相关改动

不同任务必须采用不同风险等级。

### 4.1 低风险任务

包括：

* 文案修正
* 样式微调
* 空状态优化
* 测试补充
* 文档同步
* 小范围 bug 修复

要求：

* 控制 diff。
* 不改业务状态机。
* 不改数据库字段语义。
* 不引入新依赖，除非必要。

### 4.2 中风险任务

包括：

* 新增页面
* 新增云函数 action
* 修改权限判断
* 修改订单/退款/评价/消息流程
* 修改商家、师傅、管理员相关逻辑

要求：

* 阅读相关 schema 和 contract。
* 更新测试。
* 更新文档。
* 保持旧数据兼容。

### 4.3 高风险任务

包括：

* 真实支付
* 真实退款
* 提现
* 分账
* 保证金真实支付/退款
* 实名认证
* 营业执照认证
* OCR
* 保险核验
* 真实风控
* 订单状态机重构
* 财务流水重构
* 发布包构建规则变更
* 管理员权限模型变更

要求：

* 不得在普通任务中顺手完成。
* 必须有独立阶段说明。
* 必须有明确验收标准。
* 必须更新 `docs/PHASE_CURRENT.md`。
* 必须补充专项测试。
* 必须说明 mock 与真实能力边界。

---

## 5. mock 与真实能力原则

本工程允许存在 mock 能力，但必须清晰标注。

### 5.1 通用原则

如果一个功能仍是 mock、内部模拟、占位、人工审核、预留接口，文档和代码命名都不得暗示其已经真实上线。

推荐表达：

* mock 支付
* 模拟退款
* 内部模拟流水
* UI 占位
* 预留入口
* 人工审核流程
* 测试用回调

禁止误导表达：

* 已接入真实支付
* 已完成真实退款
* 真实资金流水
* 自动风控已上线
* 资质认证已接入
* AI 派单已上线

### 5.2 阶段切换规则

当某个 mock 能力进入真实接入阶段时，必须同步更新：

* `docs/PHASE_CURRENT.md`
* `docs/PROJECT_STATUS.md`
* 相关 contract 文档
* 相关测试
* 发布检查清单
* 回滚方案
* 风险说明

不得只改代码，不改文档。

---

## 6. 前端开发原则

### 6.1 页面不得直接操作核心数据

小程序页面不应直接操作云数据库核心集合。
应优先通过 `miniprogram/services/*` 调用云函数。

前端不得直接决定：

* 支付成功
* 退款成功
* 订单完成
* 收益结算
* 财务流水生成
* 资质审核通过
* 角色权限提升
* 风控解除

### 6.2 UI 重构不得改变业务语义

UI 重构允许：

* 优化布局
* 统一视觉
* 增加 loading
* 增加 error state
* 增加 empty state
* 优化按钮层级
* 优化文案表达

UI 重构不得顺手改变：

* 订单状态
* 支付状态
* 退款状态
* 权限判断
* 金额单位
* 数据库字段含义
* 云函数 action
* 返回结构

如果 UI 上新增筛选、推荐、派单、距离、智能排序等入口，但后端没有真实逻辑，必须标注为占位或演示，不得写成已生效。

---

## 7. 云函数开发原则

### 7.1 后端必须做权限校验

云函数必须校验：

* 调用者身份
* 调用者角色
* 资源归属
* 状态是否允许流转
* 金额是否一致
* 请求是否幂等
* 管理员权限是否有效

不得信任前端传来的 openid、role、amount、status。

### 7.2 状态变化必须可追踪

涉及关键状态变化时，应保持日志、消息或审计记录。

关键状态包括：

* 订单状态
* 支付状态
* 退款状态
* 商家审核状态
* 师傅审核状态
* 资质状态
* 保证金状态
* 风控状态
* 收益状态
* 财务流水状态

### 7.3 避免无节制扩大 handler

如果单个 handler 已经过长，新功能应优先抽取同目录纯函数模块。

推荐抽取：

* 校验函数
* 格式化函数
* 状态转换函数
* 消息构造函数
* 日志构造函数
* 金额计算函数

不得为了重构而大面积改动无关逻辑。

---

## 8. 数据模型原则

修改数据字段前，必须检查：

* `schema/`
* `docs/contracts/`
* 相关云函数
* 相关前端 service
* 相关测试
* 旧数据兼容性

要求：

* 不随意删除字段。
* 新增字段必须有默认值或兼容逻辑。
* 金额单位必须统一。
* 状态枚举必须集中维护。
* 时间字段格式应沿用现有习惯。
* 不在不同模块中重复发明字段名。

---

## 9. 角色与权限原则

角色、身份、入口、消息、订单可见性必须清晰隔离。

Agent 修改角色相关逻辑时必须检查：

* 角色枚举
* 当前身份状态
* 页面入口
* 消息筛选
* 订单筛选
* 云函数权限
* 管理端操作权限
* 测试覆盖

不得只在前端隐藏按钮作为权限控制。
后端必须有对应权限校验。

---

## 10. 发布与交付原则

公开交付包必须是 clean candidate。
不得直接压缩整个工作目录。

公开交付包不得包含：

* `.git/`
* `node_modules/`
* 临时目录
* 历史参考工程
* 本地私有配置
* `.env`
* 密钥
* 证书
* 压缩包
* 真实 AppID
* 真实 openid
* 真实支付配置
* 真实地图密钥
* 真实用户数据
* 真实订单导出数据

发布前必须运行发布风险扫描。
如果扫描失败，不得声称交付包可发布。

---

## 11. 测试原则

修改后必须根据任务范围运行测试。

基础命令：

```bash
npm test
npm run check:shared-sync
npm run check:cloudfunction-deps
```

涉及发布或交付时：

```bash
npm run check:release-risk -- <candidate-dir>
```

如果没有运行测试，必须明确说明原因。
不得伪造测试结果。

如果测试数量变化，必须说明原因，并更新 `docs/PROJECT_STATUS.md` 或阶段记录。

---

## 12. 文档更新原则

以下内容变化时，必须同步更新文档：

* 当前阶段目标
* 已完成功能
* mock/真实能力边界
* 测试基线
* 发布流程
* 数据字段
* 接口契约
* 权限规则
* 支付/退款/财务流程
* 认证/资质/风控流程
* 重大工程决策

文档应描述真实状态，不得为了显得完成度高而夸大功能。

---

## 13. 阶段推进机制

进入新工程阶段时，应优先更新：

1. `docs/PHASE_CURRENT.md`
2. `docs/PROJECT_STATUS.md`
3. 必要时新增 `docs/dev-records/<phase>.md`
4. 必要时新增 `docs/DECISIONS/<date>-<topic>.md`

只有当 Agent 的长期行为规则变化时，才更新 `AGENT.MD`。

### 13.1 每个阶段必须写清楚

每个阶段至少说明：

* 阶段名称
* 阶段目标
* 本阶段允许修改什么
* 本阶段禁止修改什么
* 是否涉及高风险能力
* mock 与真实能力边界
* 验收命令
* 验收标准
* 回滚或降级策略
* 阶段完成后需要更新哪些文档

---

## 14. 冲突处理

如果用户需求、阶段文档、README、代码现状之间冲突，Agent 必须：

1. 指出冲突。
2. 判断风险等级。
3. 优先选择低风险、可回滚方案。
4. 不确定时不要擅自接入高风险能力。
5. 如果必须继续，先记录假设。

涉及真实支付、真实退款、提现、分账、认证、风控、发布安全时，不允许基于猜测继续。

---

## 15. Agent 输出要求

每次修改完成后，Agent 必须输出：

1. 本次任务类型。
2. 修改了哪些文件。
3. 每个文件为什么修改。
4. 哪些功能保持不变。
5. 是否涉及支付、退款、财务、认证、风控。
6. 如果涉及，说明仍是 mock、内部模拟，还是已进入真实接入阶段。
7. 运行了哪些命令。
8. 测试结果。
9. 是否更新了文档。
10. 是否存在未解决风险。
11. 下一步建议。

---

## 16. 总原则

Agent 的目标不是尽可能多地改代码，而是让工程持续可维护。

优先级顺序：

```text
安全边界
> 数据正确性
> 权限正确性
> 测试稳定性
> 交付干净
> 用户体验
> 开发速度
```

任何修改都应符合：

* 小步可回滚
* 不夸大 mock 能力
* 不隐藏测试失败
* 不制造无关 diff
* 不破坏旧数据
* 不绕过后端权限
* 不把当前阶段以外的能力偷偷做掉

# 微信真实环境问题清单

本文用于记录微信开发者工具、真机预览、体验版回归中发现的问题。每条问题都应记录复现环境、影响范围、修复状态和验证结果，避免发布前遗漏。

## 记录模板

| 字段     | 说明                         |
| -------- | ---------------------------- |
| 问题编号 | 使用 `ISSUE-001` 递增        |
| 发现时间 | 发现问题的日期和时间         |
| 发现环境 | 开发者工具、真机预览或体验版 |
| 账号角色 | 用户、师傅、管理员           |
| 复现步骤 | 用 1、2、3 描述最短复现路径  |
| 期望结果 | 应该出现的行为               |
| 实际结果 | 实际出现的行为               |
| 影响范围 | 阻断、严重、一般、轻微       |
| 处理状态 | 待确认、处理中、已修复、暂缓 |
| 修复说明 | 关联代码变更或处理方式       |
| 回归结果 | 回归日期、环境和结论         |

## 当前问题

| 问题编号  | 发现时间   | 发现环境   | 账号角色 | 复现步骤                                                         | 期望结果                                         | 实际结果                       | 影响范围 | 处理状态 | 修复说明                                 | 回归结果 |
| --------- | ---------- | ---------- | -------- | ---------------------------------------------------------------- | ------------------------------------------------ | ------------------------------ | -------- | -------- | ---------------------------------------- | -------- |
| ISSUE-001 | 2026-05-30 | 待真机验证 | 全部     | 按发布前检查清单完成真实 AppID、云环境和集合初始化后回归核心链路 | 用户下单、师傅接单、管理员审核、评价完成均可闭环 | 尚未在真实微信环境完成统一记录 | 一般     | 待确认   | 本地测试已覆盖关键风险，仍需真机环境验证 | 待补充   |

## 发布前重点观察项

- AppID 已替换为真实小程序 AppID，云环境选择正确。
- 云函数已上传并部署，且依赖已安装。
- `users`、`orders`、`workers`、`reviews`、`messages`、`service_categories`、`services`、`admin_operation_logs`、`payment_logs`、`after_sales`、`refund_logs` 集合可读写。
- 管理员初始化入口可用，初始化后不暴露调试能力给普通用户。
- 用户、师傅、管理员三类账号 openid 区分清楚，测试数据不混用。
- 消息点击可进入对应订单详情，并将消息标记为已读。
- 服务分类和服务删除保护符合预期，已有业务记录时优先下架而不是删除。
- 售后申请、管理员审核、模拟退款和退款日志符合预期；真实退款未配置前不得误判为真实资金退款。

<view class="empty-state">
  <view class="empty-state__mark"></view>
  <view class="empty-state__title">{{ title }}</view>
  <view wx:if="{{ description }}" class="empty-state__desc"
    >{{ description }}</view
  >
</view>

<view class="review-page ui-page ui-page--dense">
  <view class="ui-header">
    <view class="ui-title">{{ title }}</view>
    <view class="ui-subtitle">给本次服务打分，帮助后续服务更稳定</view>
  </view>

  <view class="review-panel ui-card">
    <view class="form-label">评分</view>
    <view class="rating-row">
      <view
        wx:for="{{ ratingOptions }}"
        wx:key="*this"
        class="rating-item {{ item <= rating ? 'rating-item--active' : '' }}"
        data-rating="{{ item }}"
        bindtap="selectRating"
      >
        {{ item }}
      </view>
    </view>

    <view class="form-item">
      <text class="form-label">评价内容</text>
      <textarea
        class="form-textarea"
        value="{{ content }}"
        placeholder="可填写服务态度、准时情况、完成效果"
        bindinput="handleContentInput"
      ></textarea>
    </view>
  </view>

  <button
    class="submit-button ui-primary-button"
    loading="{{ submitting }}"
    disabled="{{ submitting }}"
    bindtap="handleSubmit"
  >
    提交评价
  </button>
</view>
